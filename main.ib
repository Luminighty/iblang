extern putchar(c: char)

struct Foo {
	x: int,
	v: char,
}

fn print_foo(foo: Foo) {
	putchar('0' + foo.x);
	putchar(foo.v);
	putchar('\n');
}

fn main(): int {
	let f = Foo {
		x: 2,
		v: 'Q',
	};
	let g = *&f;
	print_foo(g);
	return 0;
}

