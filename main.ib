extern putchar(c: char)


fn print_ptr(a: *char) {
	putchar(*a);
}

fn print_arr(a: *char) {
	putchar(a[0]);
}

fn main(): int {
	let a_arr = ['a', 'b', 'c'];
	let b = 'd';
	print_ptr(a_arr);
	print_ptr(&b);
	print_arr(a_arr);
	print_arr(&b);
	return 0;
}
