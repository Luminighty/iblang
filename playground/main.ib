struct Block {
	abc: *any,
	bar: u32,
}

struct VarDecl {
	is_const: bool,
	ident: str,
	value: *any,
}

union NodeData {
	other: *any,
	block: Block,
	var: VarDecl,
}

fn main() {
	let data = NodeData {};
	let data = NodeData { block: Block{} };
	data.block.bar;
	let s = sizeof(NodeData);
}
