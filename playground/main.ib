
struct Player {
	pos: Vec2,
	stats: Stats,
	name: str,
}

struct Stats {
	health: u16,
	attack: u16,
	magic: u16,
}

struct Vec2 {
	x: f32,
	y: f32,
}

fn main() {
	let v = Vec2 { x: 10, y: 0 };
	let x = v.x;
	let v = Vec2 { };
	let v = sizeof(Vec2);
	let v = sizeof(Stats);
	let v = sizeof(Player);
	let i = sizeof(u8);
}

