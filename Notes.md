For future compiler
- [ ] Support multiple span per node
  - Things like if_cond if_body
- ExprType on struct instead of variant
- printf does not work due to the padding in chars (since I'm using W :( )
